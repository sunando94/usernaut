nameSuffix: -prod

namespace: usernaut

resources:
  - ../../base

patches:
  - path: patches/manager-deployment-env.yaml
  - patch: |-
      $patch: delete
      apiVersion: apiextensions.k8s.io/v1
      kind: CustomResourceDefinition
      metadata:
        name: .*
    target:
      group: apiextensions.k8s.io
      version: v1
      kind: CustomResourceDefinition
      name: .*
